type TreePanelModelType = {
  tabActive: number,
  expandedNodes: string[][],
  selectedNodes: string[][],
  selectedNodesProcessed: string[][],
  addChartMenu: boolean,
  nodeInfoModal: boolean,
  ideasModal: boolean,
  idTree: string[],
  allCharts: string[]
}

export default TreePanelModelType;